#include<iostream>
using namespace std;
void add(int a, double b)
{
    cout<<"sum(int+double) : "<<(a+b);
} 
void  add(double a, int b)
{
    cout<<"\nsum(double+int) : "<<(a+b);
} 
int main()
{
    add(10,2.5);
    add(5.5,6);
 
      return 0;
}
