 /*given an array of n positive integers. task is to remove the element from the range a position to b position.
After removing print the all elements left.
->6
1 4 6 2 8 7                     
1 8 7
<-2 4
[In simple input 1, we remove the elements from 2 positions to 4 positions.] */
#include<bits/stdc++.h>
using namespace std;
int main ()
{
    int n;
    cin>>n;
    vector<int> ary;
    for(int i=0;i<n;i++)
    {
        int k;
        cin>>k;
        ary.push_back(k);
    }
    int s,f;
    cin>>s>>f;
    for(int i=0 ;i<(f-s)+1; i++)
    {
      ary.erase(ary.begin()+s-1);
    }
    for (int i=0;i<ary.size();i++)
    {
        cout << ary[i]<<" ";
    }
}

